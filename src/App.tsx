import React, { useState, useEffect } from 'react';import { Heart, Calendar, MapPin, Clock, Star, Music, ChevronDown, Globe } from 'lucide-react';import InvitationDetails from './components/InvitationDetails';import RSVPForm from './components/RSVPForm';import AIBlessing from './components/AIBlessing';type Language = 'en' | 'zh';const App: React.FC = () => {  const [activeSection, setActiveSection] = useState<string>('hero');  const [isVisible, setIsVisible] = useState(false);  const [language, setLanguage] = useState<Language>('zh');  useEffect(() => {    setIsVisible(true);  }, []);  const scrollTo = (id: string) => {    const element = document.getElementById(id);    if (element) {      element.scrollIntoView({ behavior: 'smooth' });    }  };  const toggleLanguage = () => {    setLanguage(prev => prev === 'en' ? 'zh' : 'en');  };  return (    <div className="min-h-screen text-slate-800 selection:bg-rose-200">      {/* Language Toggle */}      <button         onClick={toggleLanguage}        className="fixed top-6 right-6 z-50 bg-white/80 backdrop-blur-md p-3 rounded-full shadow-lg border border-rose-100 text-rose-600 hover:bg-rose-50 transition-all duration-300 group"        aria-label="Toggle Language"      >        <Globe size={24} className="group-hover:rotate-12 transition-transform" />        <span className="absolute right-full mr-3 top-1/2 -translate-y-1/2 bg-slate-800 text-white text-xs px-2 py-1 rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap pointer-events-none">          {language === 'en' ? '切換至中文' : 'Switch to English'}        </span>      </button>      {/* Hero Section */}      <section id="hero" className="relative bg-[#fffaf5]">        <div className="absolute inset-0 opacity-10 pointer-events-none overflow-hidden">          <div className="absolute top-10 left-10 text-rose-600 rotate-12"><Heart size={80} fill="currentColor" /></div>          <div className="absolute bottom-20 right-20 text-rose-600 -rotate-12"><Heart size={60} fill="currentColor" /></div>          <div className="absolute top-1/2 left-1/4 text-rose-200"><Heart size={120} fill="currentColor" /></div>          <div className="absolute bottom-1/3 right-1/4 text-rose-200"><Heart size={100} fill="currentColor" /></div>        </div>        <div className={`transition-opacity duration-1000 ${isVisible ? 'opacity-100' : 'opacity-0'} relative z-10`}>          <div className="w-full sm:max-w-xl sm:mx-auto sm:pt-10 sm:px-6">            <div className="relative w-full aspect-[1800/1404] overflow-hidden rounded-none sm:rounded-3xl shadow-none sm:shadow-2xl border-0 sm:border-4 border-white">              <img                 src="/cover.jpg"                alt="Queenie & Kenny"                className="w-full h-full object-cover"                onError={(e) => {                  e.currentTarget.src = "https://picsum.photos/800/600?blur=2";                }}              />            </div>          </div>          <div className="text-center px-6 pt-7 pb-12 max-w-md mx-auto">            <h1 className="font-serif-luxury text-4xl md:text-5xl font-bold text-slate-900 mb-3">              {language === 'en' ? <>Queenie <span className="text-rose-600">&</span> Kenny</> : <>鄧國健 <span className="text-rose-600">&#38;</span> 翁健宜</>}            </h1>            <p className="font-serif-luxury italic text-lg md:text-xl text-slate-500 mb-8">              {language === 'en' ? 'Are Getting Married' : '我們結婚了'}            </p>            <button               onClick={() => scrollTo('details')}              className="inline-flex items-center justify-center px-8 py-3 font-semibold text-white bg-rose-600 rounded-full hover:bg-rose-700 transition-all duration-200 shadow-lg shadow-rose-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-rose-500"            >              {language === 'en' ? 'See Invitation Details' : '查看婚禮詳情'}              <ChevronDown className="ml-2 animate-bounce" />            </button>          </div>        </div>      </section>      {/* Details Section */}      <section id="details" className="py-20 bg-white">        <div className="max-w-4xl mx-auto px-4">          <InvitationDetails language={language} />        </div>      </section>      {/* RSVP Section */}      <section id="rsvp" className="py-24 bg-slate-900 text-white relative">        <div className="max-w-2xl mx-auto px-4 relative z-10">          <RSVPForm language={language} />        </div>        <div className="absolute bottom-0 right-0 p-10 opacity-10">           <Heart size={200} fill="currentColor" className="text-rose-500" />        </div>      </section>      {/* Footer */}      <footer className="py-12 bg-white text-center border-t border-rose-100">        {language === 'en' ? <p className="font-script text-3xl text-rose-600 mb-2">Queenie & Kenny</p> : <p className="font-script text-3xl text-rose-600 mb-2">鄧國健 & 翁健宜</p>}        <p className="text-slate-400 text-sm tracking-widest uppercase">          {language === 'en' ? 'Thank you for being part of our story' : '感謝您參與我們的人生篇章'}        </p>      </footer>    </div>  );};export default App;
